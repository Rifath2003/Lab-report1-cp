#include<bits/stdc++.h>
using namespace std;

int main(){
    
    long long n,i,j,k ; cin>>n;
    
    
    bool ar[n+1];
    
    for( i=0; i<=n;i++)
    
    ar[i]=1;
    
    for(j=3;i<=sqrt(n);j+=2)
    
    { if(ar[j]==1)
    
   { for(k=j*j;k<=n;k=k+(2*j))
   
     ar[k]==0;
     
   }  
   
    }
   
   vector<long long>v; v.push_back(2);
   
   for(i=3;i<=n;i+=2)
   
   if(ar[i]==1) v.push_back(1);
   
  for (auto prime : v) {
        cout << prime << " ";
        
        
        
        ll solve (int x)
        
        int h=sqrt(x);
        
        for(i=0;i<prime.size( )&&prime[i]<=h;i++)
        
        if(x%prime[i]==0){
            
            while(x%prime[i]== 0){
                
                y=x/prime[i];
                
                factor.push_back(prime[i]);
                
                x=y;
                
            }}
            
            if(x>1)factor.push_back(x);
    
    
    
}